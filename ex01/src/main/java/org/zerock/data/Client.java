package org.zerock.data;

public class Client {
	
	
	/*
	 * public static void main(String args[]) { MakeData makeData = new MakeData();
	 * makeData.setStartDate("2019-06-08"); makeData.setEndDate("2019-06-10");
	 * makeData.setDailyCount(100);
	 * 
	 * //뉴스 생성 관련 설정작업 makeData.prepare();
	 * 
	 * //뉴스 생성 실행 makeData.process(); }
	 */
}